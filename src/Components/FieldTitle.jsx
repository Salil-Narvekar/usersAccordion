import React from 'react'

const FieldTitle = ({label}) => {
    return (
        <div className='text-slate-400 font-medium text-xs md:text-sm lg:text-md w-full'>{label}</div>
    )
}

export default FieldTitle